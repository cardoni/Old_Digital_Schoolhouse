# AppConfig = YAML.load(Rails.root + 'config/app_config.yml')[Rails.env].symbolize_keys!

# This was meant to be used in tandem with config/app_config.yml for easier github use with S3 keys and what not.. hasn't worked for me yet.