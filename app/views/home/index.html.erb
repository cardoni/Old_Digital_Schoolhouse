<%= raw("<h1>Welcome back, #{current_user.name.camelize}</h1>") if signed_in? %>
<h1>Everyone's Posts</h1>
<% @posts.each do |post| %>
  <div>
    <h2><%= post.title %></h2>
    <p><%= post.body %></p>
  </div>
<% end %>