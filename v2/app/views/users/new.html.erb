<div class="new-user">
  <h1 class="header">Create New User</h1>
	
  <% if User.find(session[:user]).classification == "administrator" %>
		<%= render 'form' %>
		<% else %>
		<%= render 'formnotice' %>
	<% end %>
	
</div>